//- modal
mixin modal(title,id,custom_btn,custom_btn_id)
	.modal.fade(id=id,tabindex="-1",role="dialog",aria-labelledby=id+"Label" aria-hidden="true")
		.modal-dialog
			.modal-content
				.modal-header
					button.close(type="button",data-dismiss="modal")
						span(aria-hidden="true") &times;
						span.sr-only Close
					h4.modal-title(id=id+"Label")= title
				.modal-body
					block
				.modal-footer
					if custom_btn
						button.btn.btn-primary(type="button",id=custom_btn_id)= custom_btn
					button.btn.btn-default(type="button",data-dismiss="modal") Close

